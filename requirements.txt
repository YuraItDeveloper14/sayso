flask==3.0.3
faster-whisper==1.0.3
# faster-whisper 1.0.3 was built against huggingface-hub 0.x and imports
# `requests` directly; hub 1.x dropped that dependency, so both are pinned.
huggingface-hub>=0.23,<1.0
requests>=2.31
sounddevice==0.4.7
numpy==1.26.4
pynput==1.7.7
pyttsx3==2.90

# Optional: only needed for MCP connectors (Notion and friends). SaySo runs
# fine without it - the local file and webhook connectors do not use it.
mcp>=1.2.0

# Optional: a neural voice that runs offline on the CPU and sounds far better
# than the built-in Windows one. Without it SaySo falls back to SAPI5.
# Fetch a voice once with:
#   python -m piper.download_voices en_US-lessac-medium --download-dir models/piper
piper-tts>=1.6.0
